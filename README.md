# BMI-calculator This project is a BMI (Body Mass Index) Calculator written in Python.
It calculates the BMI of a user using their weight (in kilograms) and height (in meters) and then displays the health category based on the BMI value
